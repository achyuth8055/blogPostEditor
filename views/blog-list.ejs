<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .blog-card {
            transition: all 0.3s ease;
        }
        .blog-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }
        .gradient-text {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .prose {
            max-width: none;
        }
        .prose img {
            max-width: 100%;
            height: auto;
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    
    <header class="gradient-bg text-white py-6 md:py-8 shadow-lg">
        <div class="container mx-auto px-4">
            <div class="flex flex-col md:flex-row items-center justify-between gap-4">
                <div class="text-center md:text-left">
                    <h1 class="text-3xl md:text-5xl font-bold mb-2">CCTA Blog</h1>
                    <p class="text-white/90 text-base md:text-lg">Explore our latest articles and insights</p>
                </div>
                <a href="/home" class="flex items-center gap-2 bg-white/20 hover:bg-white/30 px-4 md:px-6 py-2 md:py-3 rounded-lg transition-all duration-200 backdrop-blur-sm text-sm md:text-base">
                    <span class="material-symbols-outlined">edit_note</span>
                    <span class="font-medium">Admin Dashboard</span>
                </a>
            </div>
        </div>
    </header>

    <main class="container mx-auto px-4 py-12">
        <% if (posts.length === 0) { %>
            
            <div class="text-center py-20">
                <div class="inline-flex items-center justify-center w-24 h-24 bg-purple-100 rounded-full mb-6">
                    <span class="material-symbols-outlined text-purple-600" style="font-size: 48px;">article</span>
                </div>
                <h2 class="text-3xl font-bold text-gray-800 mb-4">No Posts Yet</h2>
                <p class="text-gray-600 text-lg mb-8">Be the first to create amazing content!</p>
                <a href="/home" class="inline-flex items-center gap-2 bg-gradient-to-r from-purple-600 to-indigo-600 text-white px-8 py-4 rounded-lg hover:from-purple-700 hover:to-indigo-700 transition-all duration-200 font-semibold shadow-lg hover:shadow-xl">
                    <span class="material-symbols-outlined">add</span>
                    Create First Post
                </a>
            </div>
        <% } else { %>
            
            <div class="mb-8 md:mb-12">
                <div class="bg-white rounded-xl shadow-lg p-4 md:p-6">
                    <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
                        <div class="flex items-center gap-3 md:gap-4">
                            <div class="w-12 h-12 md:w-14 md:h-14 bg-gradient-to-br from-purple-500 to-indigo-600 rounded-full flex items-center justify-center flex-shrink-0">
                                <span class="material-symbols-outlined text-white text-2xl md:text-3xl">article</span>
                            </div>
                            <div>
                                <h2 class="text-2xl md:text-3xl font-bold gradient-text"><%= posts.length %></h2>
                                <p class="text-gray-600 text-sm md:text-base">Published <%= posts.length === 1 ? 'Article' : 'Articles' %></p>
                            </div>
                        </div>
                        <a href="/home" class="flex items-center gap-2 text-purple-600 hover:text-purple-700 font-medium transition-colors text-sm md:text-base">
                            <span class="material-symbols-outlined">settings</span>
                            <span>Manage Posts</span>
                        </a>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% posts.forEach(post => { %>
                    <article class="blog-card bg-white rounded-xl shadow-lg overflow-hidden">
                        
                        <div class="gradient-bg h-2"></div>

                        <div class="p-6">
                            
                            <h2 class="text-2xl font-bold text-gray-900 mb-3 line-clamp-2 hover:text-purple-600 transition-colors">
                                <a href="/post/<%= post._id %>"><%= post.title %></a>
                            </h2>

                            <div class="flex flex-wrap items-center gap-2 md:gap-4 text-xs md:text-sm text-gray-500 mb-4">
                                <div class="flex items-center gap-1">
                                    <span class="material-symbols-outlined text-sm">calendar_today</span>
                                    <span><%= new Date(post.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></span>
                                </div>
                                <% 
                                if (post.updatedAt) {
                                    const createdTime = post.createdAt ? new Date(post.createdAt).getTime() : 0;
                                    const updatedTime = new Date(post.updatedAt).getTime();
                                    if (updatedTime !== createdTime) { 
                                %>
                                    <div class="flex items-center gap-1">
                                        <span class="material-symbols-outlined text-sm">update</span>
                                        <span>Updated <%= new Date(post.updatedAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %></span>
                                    </div>
                                <% 
                                    }
                                } 
                                %>
                            </div>

                            <% 
                                const tempDiv = post.content ? post.content.replace(/<[^>]*>/g, ' ').replace(/\s+/g, ' ').trim() : '';
                                const excerpt = tempDiv.length > 180 ? tempDiv.substring(0, 180) + '...' : tempDiv;
                            %>
                            <p class="text-gray-600 mb-6 line-clamp-3"><%= excerpt %></p>

                            <% if (post.focusKeyword) { %>
                                <div class="flex flex-wrap gap-2 mb-4">
                                    <span class="inline-flex items-center gap-1 px-3 py-1 bg-purple-100 text-purple-700 rounded-full text-xs font-medium">
                                        <span class="material-symbols-outlined" style="font-size: 14px;">tag</span>
                                        <%= post.focusKeyword %>
                                    </span>
                                </div>
                            <% } %>

                            <a href="/post/<%= post._id %>" class="inline-flex items-center gap-2 text-purple-600 hover:text-purple-700 font-semibold transition-colors group">
                                <span>Read Full Article</span>
                                <span class="material-symbols-outlined group-hover:translate-x-1 transition-transform">arrow_forward</span>
                            </a>
                        </div>

                        <div class="px-4 md:px-6 py-3 md:py-4 bg-gray-50 border-t border-gray-100">
                            <div class="flex items-center justify-between text-xs md:text-sm">
                                <div class="flex items-center gap-1 md:gap-2 text-gray-500">
                                    <span class="material-symbols-outlined text-sm">visibility</span>
                                    <span class="hidden sm:inline">Public View</span>
                                    <span class="sm:hidden">Public</span>
                                </div>
                                <a href="/post/<%= post._id %>" class="flex items-center gap-1 text-purple-600 hover:text-purple-700 font-medium transition-colors">
                                    <span>View</span>
                                    <span class="material-symbols-outlined text-sm">open_in_new</span>
                                </a>
                            </div>
                        </div>
                    </article>
                <% }); %>
            </div>
        <% } %>
    </main>

    <footer class="bg-white border-t border-gray-200 mt-12 md:mt-20">
        <div class="container mx-auto px-4 py-6 md:py-8">
            <div class="flex flex-col md:flex-row items-center justify-between gap-4">
                <div class="mb-4 md:mb-0 text-center md:text-left">
                    <h3 class="text-xl md:text-2xl font-bold gradient-text mb-2">CCTA Blog</h3>
                    <p class="text-gray-600 text-sm md:text-base">Powered by AI-Enhanced Content System</p>
                </div>
                <div class="flex items-center gap-4 md:gap-6 text-sm md:text-base">
                    <a href="/home" class="flex items-center gap-2 text-gray-600 hover:text-purple-600 transition-colors">
                        <span class="material-symbols-outlined">admin_panel_settings</span>
                        <span>Admin Area</span>
                    </a>
                    <a href="/blog" class="flex items-center gap-2 text-gray-600 hover:text-purple-600 transition-colors">
                        <span class="material-symbols-outlined">refresh</span>
                        <span>Refresh</span>
                    </a>
                </div>
            </div>
            <div class="mt-4 md:mt-6 pt-4 md:pt-6 border-t border-gray-200 text-center text-gray-500 text-xs md:text-sm">
                <p>&copy; <%= new Date().getFullYear() %> CCTA Blog. All rights reserved.</p>
            </div>
        </div>
    </footer>
</body>
</html>
