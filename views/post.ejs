<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Inter', sans-serif; 
        }
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .gradient-text {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        /* Article Typography */
        article h1 { 
            font-size: 2.5rem;
            margin-top: 2rem;
            margin-bottom: 1rem;
            font-weight: 800;
            line-height: 1.2;
            color: #1a202c;
        }
        article h2 { 
            font-size: 2rem;
            margin-top: 2.5rem;
            margin-bottom: 0.875rem;
            font-weight: 700;
            line-height: 1.3;
            color: #2d3748;
        }
        article h3 { 
            font-size: 1.5rem;
            margin-top: 2rem;
            margin-bottom: 0.75rem;
            font-weight: 600;
            line-height: 1.4;
            color: #2d3748;
        }
        article h4 { 
            font-size: 1.25rem;
            margin-top: 1.5rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
            line-height: 1.5;
            color: #4a5568;
        }
        article p { 
            margin-bottom: 1.25rem;
            line-height: 1.8;
            color: #4a5568;
            font-size: 1.0625rem;
        }
        
        /* List Styling - Critical for bullets and numbering */
        article ul { 
            margin: 1.25rem 0;
            padding-left: 2rem;
            list-style-type: disc !important;
            list-style-position: outside !important;
            display: block !important;
            color: #4a5568;
        }
        article ol { 
            margin: 1.25rem 0;
            padding-left: 2rem;
            list-style-type: decimal !important;
            list-style-position: outside !important;
            display: block !important;
            color: #4a5568;
        }
        article li {
            margin-bottom: 0.5rem;
            line-height: 1.7;
            display: list-item !important;
            padding-left: 0.25rem;
        }
        
        /* Nested list styles */
        article ul ul {
            list-style-type: circle !important;
            margin: 0.5rem 0;
        }
        article ul ul ul {
            list-style-type: square !important;
        }
        article ol ol {
            list-style-type: lower-alpha !important;
            margin: 0.5rem 0;
        }
        article ol ol ol {
            list-style-type: lower-roman !important;
        }
        
        /* Force list styles even with Tailwind resets */
        article ul[class*="list-none"] {
            list-style-type: disc !important;
        }
        article ol[class*="list-none"] {
            list-style-type: decimal !important;
        }
        article ul > li:before,
        article ol > li:before {
            content: none !important;
        }
        
        article blockquote { 
            border-left: 4px solid #667eea;
            padding-left: 1.5rem;
            padding-top: 0.5rem;
            padding-bottom: 0.5rem;
            font-style: italic;
            color: #4a5568;
            margin: 1.5rem 0;
            background-color: #f7fafc;
            border-radius: 0 0.375rem 0.375rem 0;
        }
        article a {
            color: #667eea;
            text-decoration: underline;
            transition: color 0.2s;
        }
        article a:hover {
            color: #764ba2;
        }
        article img {
            max-width: 100%;
            height: auto;
            border-radius: 0.5rem;
            margin: 1.5rem 0;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        article pre {
            background-color: #2d3748;
            color: #e2e8f0;
            padding: 1.25rem;
            border-radius: 0.5rem;
            overflow-x: auto;
            margin: 1.5rem 0;
            font-size: 0.875rem;
        }
        article code {
            background-color: #edf2f7;
            color: #e53e3e;
            padding: 0.125rem 0.375rem;
            border-radius: 0.25rem;
            font-size: 0.875em;
        }
        article pre code {
            background: none;
            color: inherit;
            padding: 0;
        }
        article strong {
            font-weight: 700;
            color: #2d3748;
        }
        article em {
            font-style: italic;
        }
        article hr {
            border: none;
            border-top: 2px solid #e2e8f0;
            margin: 2.5rem 0;
        }
        article table {
            width: 100%;
            border-collapse: collapse;
            margin: 1.5rem 0;
        }
        article th, article td {
            border: 1px solid #e2e8f0;
            padding: 0.75rem;
            text-align: left;
        }
        article th {
            background-color: #f7fafc;
            font-weight: 600;
            color: #2d3748;
        }
        
        /* Enable text selection and context menu */
        article * {
            user-select: text !important;
            -webkit-user-select: text !important;
            -moz-user-select: text !important;
            -ms-user-select: text !important;
        }
        
        .back-button {
            transition: all 0.3s ease;
        }
        .back-button:hover {
            transform: translateX(-4px);
        }
        .edit-button {
            transition: all 0.3s ease;
        }
        .edit-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(102, 126, 234, 0.2);
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    
    <nav class="bg-white shadow-md sticky top-0 z-50">
        <div class="max-w-5xl mx-auto px-4 py-3 md:py-4">
            <div class="flex justify-between items-center gap-2">
                
                <a href="/blog" class="back-button flex items-center gap-1 text-purple-600 hover:text-purple-700 font-semibold text-sm md:text-base flex-shrink-0">
                    <span class="material-symbols-outlined text-lg md:text-xl">arrow_back</span>
                    <span class="hidden sm:inline">Back to All Posts</span>
                    <span class="sm:hidden">Back</span>
                </a>

                <div class="flex items-center gap-2 flex-shrink-0">
                    <span class="text-lg md:text-2xl font-bold gradient-text">CCTA Blog</span>
                </div>

                <div class="flex items-center gap-1 md:gap-2 flex-shrink-0">
                    <a href="/home" class="flex items-center gap-1 px-2 md:px-3 py-2 text-gray-600 hover:text-purple-600 transition-colors text-xs md:text-sm">
                        <span class="material-symbols-outlined text-sm md:text-base">dashboard</span>
                        <span class="hidden lg:inline">Admin</span>
                    </a>
                    <a href="/?postId=<%= postId %>" class="edit-button flex items-center gap-1 px-2 md:px-4 py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-all text-xs md:text-sm font-medium shadow-md">
                        <span class="material-symbols-outlined text-sm md:text-base">edit</span>
                        <span class="hidden sm:inline">Edit</span>
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-4xl mx-auto px-4 py-8 md:py-12">
        
        <div class="mb-8">
            <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6"><%= postTitle %></h1>

            <div class="flex flex-wrap items-center gap-3 md:gap-4 text-xs md:text-sm text-gray-600 pb-6 border-b border-gray-200">
                <% if (createdAt) { %>
                    <div class="flex items-center gap-1 md:gap-2">
                        <span class="material-symbols-outlined text-purple-600" style="font-size: 18px;">calendar_today</span>
                        <span class="font-medium">Published:</span>
                        <span><%= new Date(createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                    </div>
                <% } %>
                <% if (updatedAt) { 
                    const createdTime = createdAt ? new Date(createdAt).getTime() : 0;
                    const updatedTime = new Date(updatedAt).getTime();
                    if (updatedTime !== createdTime) { 
                %>
                    <div class="flex items-center gap-1 md:gap-2">
                        <span class="material-symbols-outlined text-purple-600" style="font-size: 18px;">update</span>
                        <span class="font-medium">Updated:</span>
                        <span><%= new Date(updatedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                    </div>
                <% 
                    }
                } 
                %>
            </div>
        </div>

        <article class="bg-white rounded-xl shadow-lg p-6 md:p-10 mb-8">
            <section class="prose max-w-none" id="post-content">
                <%- postContent %>
            </section>
        </article>

        <div class="bg-white rounded-xl shadow-md p-4 md:p-6">
            <div class="flex flex-col gap-3">
                
                <a href="/blog" class="back-button flex items-center justify-center gap-2 text-purple-600 hover:text-purple-700 font-semibold py-2 md:hidden">
                    <span class="material-symbols-outlined">arrow_back</span>
                    <span>View All Posts</span>
                </a>

                <div class="hidden md:flex items-center justify-between">
                    <a href="/blog" class="back-button flex items-center gap-2 text-purple-600 hover:text-purple-700 font-semibold">
                        <span class="material-symbols-outlined">arrow_back</span>
                        <span>View All Posts</span>
                    </a>
                    <div class="flex gap-3">
                        <a href="/home" class="flex items-center gap-2 px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors">
                            <span class="material-symbols-outlined text-sm">dashboard</span>
                            <span>Admin Dashboard</span>
                        </a>
                        <a href="/?postId=<%= postId %>" class="edit-button flex items-center gap-2 px-4 py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-all shadow-md">
                            <span class="material-symbols-outlined text-sm">edit_square</span>
                            <span>Edit Post</span>
                        </a>
                    </div>
                </div>

                <div class="flex gap-2 md:hidden">
                    <a href="/home" class="flex-1 flex items-center justify-center gap-2 px-3 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors text-sm">
                        <span class="material-symbols-outlined text-sm">dashboard</span>
                        <span>Dashboard</span>
                    </a>
                    <a href="/?postId=<%= postId %>" class="flex-1 edit-button flex items-center justify-center gap-2 px-3 py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-all shadow-md text-sm">
                        <span class="material-symbols-outlined text-sm">edit_square</span>
                        <span>Edit Post</span>
                    </a>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-white border-t border-gray-200 mt-16">
        <div class="max-w-5xl mx-auto px-4 py-8">
            <div class="flex flex-col md:flex-row items-center justify-between">
                <div class="mb-4 md:mb-0 text-center md:text-left">
                    <h3 class="text-2xl font-bold gradient-text mb-2">CCTA Blog</h3>
                    <p class="text-gray-600">AI-Powered Content Platform</p>
                </div>
                <div class="text-gray-500 text-sm">
                    <p>&copy; <%= new Date().getFullYear() %> CCTA Blog. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>
